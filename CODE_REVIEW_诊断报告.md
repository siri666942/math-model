# C题 Python代码走查报告

对照《技术报告_烟幕干扰弹代码重写方案.md》里约定的模块划分和各问目标区间，对 `py_solution/` 做了一次逐文件核对。结论：仿真物理/遮蔽判据本身没有错，问题出在工程实现上——一是有一处会让脚本直接崩溃的低级bug，二是真正跑出 `README.md` 里那份结果的，其实不是报告里设计的那套PSO流水线，而是学弟另外写的一套偷工减料的简化搜索。

## 1. 问题1结果 0.0000s —— 核实后确认不是bug

`simulation.py` 里 `check_occlusion` 的视锥半顶角判据，逐条对照 `支撑材料/m_1_1.m`（老代码里的参考MATLAB实现），公式完全一致（视线偏离角、`dist_mt<=dist_st` 的"云团在导弹与目标之间"判断都是照抄参考实现，不是自造的）。

用原2025A题参数（投放1.5s/延时3.6s/下沉3m/s）重放这套算法，复现出 **1.392s**，和《国一国二对比与数字讨论报告》给出的共识区间 1.39~1.41s 吻合，说明算法本身是对的。

换成moni2026的新参数（投放1.2s/延时3.2s/下沉2.5m/s）后，用完整的8640个关键点验证：导弹飞行全程离云团最近只有 **10.73m**（有效遮蔽半径10m），只差0.73m没有真正穿过云团——这是这三个参数改动叠加后产生的一个真实的贴边情况，不是代码逻辑错了。

**建议**：论文里针对问题1加一句敏感性说明（例如"以给定参数计算，遮蔽窗口趋近于0，缘于导弹航线与云团中心的最近距离(10.73m)略大于有效遮蔽半径(10m)"），不需要去"修"这个数字。

## 2. 真正的bug：项目里有两条流水线，`README.md`里的结果来自那条错的

项目里其实并存两套完全独立的求解代码：

| | 架构 | PSO预算 | 入口 |
|---|---|---|---|
| **A. `problem1~5.py` + `pso.py`** | 按技术报告设计：完整PSO联合优化全部决策变量 | 按问题规模递增：200×100 → 300×150 → 500×300 → 300×200 | `main.py` |
| **B. `run_all_v2.py` / `final_solve.py`** | 学弟另写的简化版：随机采样"猜"一个爆炸点，反推航向/速度，再局部微调 | 3~5万次随机采样 | 直接运行 |

`README.md`"最终结果一览"表里的数字（问题3: 4.9s、问题4: 8.3s、问题5: 5.8s，全面低于技术报告的目标区间）经比对，**来源是B，不是A**。`py_solution/__pycache__/` 里能找到 `fast_search`、`grid_search`、`run_all_v2` 的编译缓存，但没有 `problem1~5`、`final_solve` 的——也印证了实际跑出结果、生成 `result*.xlsx` 的是B这条路。

B这套简化搜索有两个实质性缺陷，直接拉低了问题3/4/5的分数：

1. **速度基本锁死在120m/s**：`run_all_v2.py` 第65行 `for v_test in [v_max, 115, 110, 100, v_min]: ...; break`——按从大到小的顺序找第一个可行速度就跳出循环，而120m/s（`v_max`）在几何上几乎总是可行的，所以速度这个决策变量实际上从没被真正搜索过，等于把4维（或8维/40维）问题白白砍掉了一维。
2. **多弹/多机场景不做联合优化**：问题3（3弹）、问题4（3机）、问题5（5机15弹）场景下，B的做法是先针对"单弹打单枚导弹"这个子问题各自独立搜出一组`(航向,速度)`，然后**冻结**这组值，只在投放时间/延时上做微调或不调（问题5里甚至直接对3枚弹各自搜出的航向/速度取中位数）。而A（`problem3/4/5.py`）是把全部变量放进同一个PSO联合优化。变量越多、越需要协同的问题，B掉分越厉害——这正好解释了为什么问题5（40维，B里被拆成"3次独立搜索+取中位数"）跌得最惨（5.8s vs 目标17~34s），问题4次之（8.3s vs 11.75s），问题3相对好一点（4.9s vs 7.65s）。

**结论**：不是建模思路错了，是学弟为了让代码能在合理时间内跑出结果，绕开了报告里设计对的PSO架构，换成了一套会系统性丢解的简化搜索，而且没有说明/标注这个替换。

## 3. 顺带发现的两处具体代码问题（已修复）

- **`problem2.py` 缺少import，一运行就崩溃**：函数里用到了 `PSO_SWARM_SIZE`、`PSO_MAX_ITER`，但文件顶部的 `from config import (...)` 没有把这两个名字导进来，直接 `NameError`。这说明A这条"架构正确"的流水线实际上**从未被成功运行过一次**——不是学弟不想按报告的设计做，是这份代码本身跑不通。已修复（在import里补上这两个名字）。

- **`simulation.py` 的核心仿真函数是纯Python双重for循环**：外层逐时间步、内层逐关键点（问题1用0.0001s步长×8640个关键点，相当于几十亿次循环），这正是技术报告里"0.1/0.2模块"明确要求"用NumPy把全部关键点×该时刻一次性算成矩阵运算"、"不要写Python for循环"的地方，但学弟的实现完全没有向量化。这也是B之所以存在的直接原因——A的PSO需要几千到几万次目标函数求值，按原来的实现方式跑一次全量问题可能要几十分钟到几小时，学弟大概率是被这个卡住了才转而写B。

  已将 `check_occlusion` 及 `simulate_single_bomb` / `simulate_multi_bomb_single_drone` / `simulate_multi_drone_multi_bomb` 三个函数重写为NumPy向量化实现（云团/导弹位置用解析式直接生成时间序列，遮蔽判据对"全部关键点×一批时刻"做矩阵运算），语义和原实现完全一致，只是把逐步累加换成了向量化计算。问题1的验证耗时从（原实现按此规模推算的）小时级降到46秒，数值结果与手工验证一致（0.0000s，见第1节分析）。

## 4. 建议的下一步

1. 用修复后的 `main.py`（会依次调用 `problem1~5.py`）重新跑一遍，产出的才是真正对得上技术报告目标区间的数字；`run_all_v2.py`/`final_solve.py` 建议标注为"废弃/仅供参考"，不要再作为最终结果来源。
2. 即便向量化之后，问题4（500×300）、问题5（两阶段，40维联合优化300×200）单次全量运行预计仍需要数十分钟量级，建议先用较小的粒子群/迭代次数跑通验证正确性，再上调到报告建议的完整预算做最终结果。
3. 问题1的0.0000s按第1节的说明写进论文即可，不需要再改代码。
